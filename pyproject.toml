[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mref-ad"
version = "0.1.0"
description = "Multimodal AD research code; editable install wires imports without PYTHONPATH."
readme = "README.md"
requires-python = ">=3.10"
